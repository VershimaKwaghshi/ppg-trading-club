import React, { useState } from "react";

// Clean, stationary Navbar with Shimmer Logo
function Navbar() {
  return (
    <nav style={{
      position: "fixed", 
      top: 0, 
      left: 0, 
      right: 0, 
      zIndex: 1000,
      background: "#050814",
      borderBottom: "1px solid rgba(196,160,80,0.15)",
      padding: "1rem 1.25rem",
      display: "flex", 
      alignItems: "center", 
      justifyContent: "center"
    }}>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "6px", userSelect: "none" }}>
        <div className="shimmer-text" style={{ 
          fontFamily: "'Playfair Display', serif", 
          fontSize: "28px", 
          fontWeight: "900", 
          fontStyle: "italic",
          letterSpacing: "2px",
          lineHeight: 1
        }}>
          Trading Club
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: "10px", width: "100%" }}>
          <div style={{ height: "1px", flex: 1, background: "linear-gradient(90deg, transparent, #c4a050)" }} />
          <div style={{ 
            fontFamily: "'DM Sans', sans-serif", 
            fontSize: "8px", 
            fontWeight: "700", 
            color: "#e8d080", 
            letterSpacing: "4px",
            textTransform: "uppercase"
          }}>
            Penny Partners Group
          </div>
          <div style={{ height: "1px", flex: 1, background: "linear-gradient(90deg, #c4a050, transparent)" }} />
        </div>
      </div>

      <style>{`
        .shimmer-text {
          background: linear-gradient(90deg, #8a6520 0%, #c4a050 25%, #f5e098 50%, #c4a050 75%, #8a6520 100%);
          background-size: 200% auto;
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          animation: goldShimmer 3s linear infinite;
        }
        @keyframes goldShimmer {
          0% { background-position: 200% center; }
          100% { background-position: -200% center; }
        }
      `}</style>
    </nav>
  );
}

export default function App() {
  return (
    <div style={{ paddingTop: "100px", background: "#050814", minHeight: "100vh" }}>
      <Navbar />
      <div style={{ color: "#fff", textAlign: "center", marginTop: "50px" }}>
        <h1>Welcome to the Trading Club</h1>
      </div>
    </div>
  );
}
